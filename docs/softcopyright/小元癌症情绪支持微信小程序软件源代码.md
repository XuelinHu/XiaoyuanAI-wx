# 小元癌症情绪支持微信小程序软件源代码

版本：V1.0

## 文件清单

- `.gitignore`
- `app.js`
- `app.json`
- `app.wxss`
- `config/index.js`
- `utils/local-data.js`
- `utils/request.js`
- `services/chat.js`
- `services/history.js`
- `services/profile.js`
- `components/bottom-tabs/index.js`
- `components/bottom-tabs/index.json`
- `components/bottom-tabs/index.wxml`
- `components/bottom-tabs/index.wxss`
- `pages/chat/index.js`
- `pages/chat/index.json`
- `pages/chat/index.wxml`
- `pages/chat/index.wxss`
- `pages/history/index.js`
- `pages/history/index.json`
- `pages/history/index.wxml`
- `pages/history/index.wxss`
- `pages/profile/index.js`
- `pages/profile/index.json`
- `pages/profile/index.wxml`
- `pages/profile/index.wxss`
- `project.config.json`
- `sitemap.json`

## 代码正文

### .gitignore

```text
project.private.config.json
```

### app.js

```javascript
App({
  globalData: {
    appName: '小元',
    backendReady: false,
  },

  onLaunch() {
    this.globalData.backendReady = true;
  },
});
```

### app.json

```json
{
  "pages": [
    "pages/chat/index",
    "pages/history/index",
    "pages/profile/index"
  ],
  "window": {
    "navigationStyle": "custom",
    "backgroundTextStyle": "light",
    "backgroundColor": "#f7efe4"
  },
  "style": "v2",
  "sitemapLocation": "sitemap.json",
  "lazyCodeLoading": "requiredComponents"
}
```

### app.wxss

```css
page {
  --bg-page: #fff7ef;
  --bg-surface: #fffdfa;
  --bg-soft-orange: #ffeddc;
  --bg-soft-blue: #e8f3ff;
  --bg-soft-gray: #f2f4f7;
  --bg-warm-card: #fff3e3;
  --brand-orange: #ff8a3d;
  --brand-orange-deep: #f26a21;
  --brand-blue: #5a9bff;
  --text-primary: #2d3748;
  --text-secondary: #667085;
  --text-light: #98a2b3;
  --border-soft: rgba(255, 138, 61, 0.18);
  --shadow-soft: 0 12rpx 36rpx rgba(58, 97, 132, 0.08);
  --shadow-bubble: 0 10rpx 24rpx rgba(90, 155, 255, 0.16);
  --radius-xl: 32rpx;
  --radius-lg: 24rpx;
  --radius-md: 18rpx;
  --gap-xs: 8rpx;
  --gap-sm: 16rpx;
  --gap-md: 24rpx;
  --gap-lg: 32rpx;
  --gap-xl: 40rpx;
  background: linear-gradient(180deg, #fff8f2 0%, #fff3e8 52%, #fff9f4 100%);
  color: var(--text-primary);
  font-size: 30rpx;
  line-height: 1.4;
}

view,
text,
button,
input,
textarea,
scroll-view {
  box-sizing: border-box;
}

.page-shell {
  min-height: 100vh;
  padding-top: calc(env(safe-area-inset-top) + 24rpx);
  padding-bottom: calc(env(safe-area-inset-bottom) + 32rpx);
}

.card {
  background: rgba(255, 253, 250, 0.92);
  border: 2rpx solid rgba(255, 255, 255, 0.55);
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-soft);
}

.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  border: 0;
}
```

### config/index.js

```javascript
const backendConfig = {
  baseUrl: 'https://api.xiaoyuan-care.com/v1',
  timeout: 8000,
  tenantCode: 'xiaoyuan-mini',
  useLocalFallback: true,
};

module.exports = {
  backendConfig,
};
```

### utils/local-data.js

```javascript
function getTimeLabel() {
  const date = new Date();
  const hours = `${date.getHours()}`.padStart(2, '0');
  const minutes = `${date.getMinutes()}`.padStart(2, '0');
  return `${hours}:${minutes}`;
}

function buildAssistantReply(content) {
  if (content.includes('害怕') || content.includes('担心')) {
    return '害怕是很真实的反应，不代表你不够勇敢。现在先把最担心的那件事说清楚，小元会陪你把它拆开来看。';
  }

  if (content.includes('睡') || content.includes('失眠')) {
    return '睡不着时情绪会被放大，先不用要求自己立刻放松。你可以先把脑子里最吵的那个念头说出来，我们一起把它放到更可处理的位置。';
  }

  if (content.includes('家人') || content.includes('爸爸') || content.includes('妈妈')) {
    return '你在意家人的感受，也说明你一直在努力保护关系。可以先把“我现在有点难受，但你不用马上替我解决”这样的话说出来，沟通会更轻一点。';
  }

  return '我看到你已经把感受说出来了，这一步很重要。你可以继续说当下最压着你的那个点，小元会接着陪你往下梳理。';
}

function createChatBootstrap() {
  return {
    conversationId: 'conv-demo-001',
    assistantProfile: {
      name: '小元',
      roleTitle: '癌症情绪支持陪伴助手',
      statusText: '在线陪伴中',
      intro:
        '小元提供情绪支持会话、历史浏览和个人偏好服务，前端已接入会话初始化、消息发送、历史查询和个人配置接口。',
    },
    messages: [
      {
        id: 101,
        role: 'assistant',
        content: '你好，我是小元。这里是你的情绪支持会话空间，你可以直接说出现在最难受的感受。',
        time: '20:30',
      },
      {
        id: 102,
        role: 'assistant',
        content: '如果你愿意，我们可以从“今天最压着你的那件事”开始聊。',
        time: '20:31',
      },
    ],
    hasMoreHistory: true,
    nextMessageId: 103,
  };
}

function createHistoryMessages() {
  return [
    {
      id: 91,
      role: 'assistant',
      content: '你不需要一个人把所有情绪都扛住，我们可以先从最难受的那部分开始。',
      time: '20:25',
    },
    {
      id: 92,
      role: 'user',
      content: '这几天总觉得很累，也不知道怎么和家里人说。',
      time: '20:26',
    },
    {
      id: 93,
      role: 'assistant',
      content: '能说出这句话已经很不容易了。我们先不急着解决全部问题，先聊你最怕他们听到什么。',
      time: '20:27',
    },
  ];
}

function createChatReply(payload) {
  const content = payload && payload.content ? payload.content : '';
  return {
    replyMessage: {
      id: Date.now(),
      role: 'assistant',
      content: buildAssistantReply(content),
      time: getTimeLabel(),
      loading: false,
    },
    conversationId: payload && payload.conversationId ? payload.conversationId : 'conv-demo-001',
    assistantStatus: '在线陪伴中',
  };
}

function createSessionList() {
  return [
    {
      id: 1,
      title: '夜间情绪安抚',
      preview: '围绕睡眠压力、复查担忧和与家人的沟通方式进行了支持会话。',
      time: '昨天 22:18',
      mood: '需要安抚',
      moodClass: 'session-tag--warm',
    },
    {
      id: 2,
      title: '复诊前焦虑整理',
      preview: '帮助梳理复诊前的担忧点，并形成待沟通问题清单。',
      time: '周四 18:46',
      mood: '慢慢平稳',
      moodClass: 'session-tag--calm',
    },
    {
      id: 3,
      title: '家庭沟通支持',
      preview: '整理与家属沟通时的表达顺序和情绪边界，减轻“怕拖累别人”的压力。',
      time: '周二 09:12',
      mood: '持续支持',
      moodClass: 'session-tag--warm',
    },
  ];
}

function createProfileCards() {
  return [
    {
      title: '会话偏好',
      desc: '设置小元回复风格、安抚节奏、重点提醒和是否优先共情。',
      action: '管理',
    },
    {
      title: '紧急支持',
      desc: '维护热线、医院联系人和一键求助入口，便于在高压场景中快速触达支持资源。',
      action: '查看',
    },
    {
      title: '消息与提醒',
      desc: '管理复诊提醒、情绪记录、睡眠观察和系统通知策略。',
      action: '配置',
    },
  ];
}

module.exports = {
  createChatBootstrap,
  createHistoryMessages,
  createChatReply,
  createSessionList,
  createProfileCards,
};
```

### utils/request.js

```javascript
const { backendConfig } = require('../config/index');

function request(options) {
  return new Promise((resolve, reject) => {
    wx.request({
      url: `${backendConfig.baseUrl}${options.url}`,
      method: options.method || 'GET',
      data: options.data || {},
      timeout: options.timeout || backendConfig.timeout,
      header: Object.assign(
        {
          'content-type': 'application/json',
          'x-tenant-code': backendConfig.tenantCode,
        },
        options.header || {}
      ),
      success(response) {
        const { statusCode, data } = response;
        if (statusCode >= 200 && statusCode < 300) {
          resolve(data);
          return;
        }
        reject({
          message: '接口请求失败',
          response,
        });
      },
      fail(error) {
        reject(error);
      },
    });
  });
}

async function requestWithFallback(options) {
  try {
    return await request(options);
  } catch (error) {
    if (!backendConfig.useLocalFallback || typeof options.fallback !== 'function') {
      throw error;
    }
    return options.fallback(error);
  }
}

module.exports = {
  request,
  requestWithFallback,
};
```

### services/chat.js

```javascript
const { requestWithFallback } = require('../utils/request');
const { createChatBootstrap, createHistoryMessages, createChatReply } = require('../utils/local-data');

function fetchConversationBootstrap() {
  return requestWithFallback({
    url: '/conversation/bootstrap',
    method: 'GET',
    fallback() {
      return createChatBootstrap();
    },
  });
}

function fetchAssistantProfile() {
  return requestWithFallback({
    url: '/assistant/profile',
    method: 'GET',
    fallback() {
      return createChatBootstrap().assistantProfile;
    },
  });
}

function fetchConversationHistory(payload) {
  return requestWithFallback({
    url: '/conversation/history',
    method: 'GET',
    data: payload,
    fallback() {
      return {
        messages: createHistoryMessages(),
        hasMoreHistory: false,
      };
    },
  });
}

function sendConversationMessage(payload) {
  return requestWithFallback({
    url: '/conversation/message',
    method: 'POST',
    data: payload,
    fallback() {
      return createChatReply(payload);
    },
  });
}

module.exports = {
  fetchConversationBootstrap,
  fetchAssistantProfile,
  fetchConversationHistory,
  sendConversationMessage,
};
```

### services/history.js

```javascript
const { requestWithFallback } = require('../utils/request');
const { createSessionList } = require('../utils/local-data');

function fetchSessionList() {
  return requestWithFallback({
    url: '/conversation/list',
    method: 'GET',
    fallback() {
      return {
        sessions: createSessionList(),
      };
    },
  });
}

module.exports = {
  fetchSessionList,
};
```

### services/profile.js

```javascript
const { requestWithFallback } = require('../utils/request');
const { createProfileCards } = require('../utils/local-data');

function fetchProfileCards() {
  return requestWithFallback({
    url: '/profile/cards',
    method: 'GET',
    fallback() {
      return {
        items: createProfileCards(),
      };
    },
  });
}

module.exports = {
  fetchProfileCards,
};
```

### components/bottom-tabs/index.js

```javascript
Component({
  properties: {
    current: {
      type: String,
      value: 'chat',
    },
  },

  data: {
    tabs: [
      { key: 'history', label: '历史', icon: 'H', url: '/pages/history/index' },
      { key: 'chat', label: '会话', icon: 'AI', url: '/pages/chat/index' },
      { key: 'profile', label: '我的', icon: 'ME', url: '/pages/profile/index' },
    ],
  },

  methods: {
    handleTap(event) {
      const { key, url } = event.currentTarget.dataset;
      if (!url || key === this.properties.current) {
        return;
      }
      wx.redirectTo({ url });
    },
  },
});
```

### components/bottom-tabs/index.json

```json
{
  "component": true
}
```

### components/bottom-tabs/index.wxml

```xml
<view class="bottom-tabs">
  <view class="tabs-shell">
    <view
      wx:for="{{tabs}}"
      wx:key="key"
      class="tab-item {{current === item.key ? 'tab-item--active' : ''}}"
      data-key="{{item.key}}"
      data-url="{{item.url}}"
      bindtap="handleTap"
    >
      <text class="tab-icon">{{item.icon}}</text>
      <text class="tab-text">{{item.label}}</text>
    </view>
  </view>
</view>
```

### components/bottom-tabs/index.wxss

```css
.bottom-tabs {
  position: fixed;
  left: 24rpx;
  right: 24rpx;
  bottom: calc(env(safe-area-inset-bottom) + 20rpx);
  z-index: 30;
}

.tabs-shell {
  display: flex;
  gap: 16rpx;
  padding: 14rpx;
  border-radius: 999rpx;
  background: rgba(255, 253, 249, 0.96);
  border: 2rpx solid rgba(255, 138, 61, 0.14);
  box-shadow: 0 18rpx 40rpx rgba(106, 66, 20, 0.14);
  backdrop-filter: blur(14rpx);
}

.tab-item {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  padding: 18rpx 12rpx;
  border-radius: 999rpx;
  color: #7a8599;
  transition: all 180ms ease;
}

.tab-item--active {
  background: linear-gradient(135deg, #ff983f 0%, #ffbd59 100%);
  color: #ffffff;
  box-shadow: 0 12rpx 24rpx rgba(255, 138, 61, 0.28);
}

.tab-icon {
  font-size: 28rpx;
  line-height: 1;
}

.tab-text {
  font-size: 24rpx;
  font-weight: 600;
}
```

### pages/chat/index.js

```javascript
const {
  fetchConversationBootstrap,
  fetchAssistantProfile,
  fetchConversationHistory,
  sendConversationMessage,
} = require('../../services/chat');

Page({
  data: {
    assistantStatus: '在线陪伴中',
    assistantIntro: '',
    conversationId: '',
    messages: [],
    draft: '',
    scrollIntoView: '',
    sending: false,
    bootstrapping: false,
    keyboardHeight: 108,
    scrollExtraHeight: 0,
    hasMoreHistory: false,
    nextMessageId: 1,
  },

  onLoad() {
    this.setupKeyboardListener();
    this.initializeConversation();
  },

  onUnload() {
    this.clearReplyTimer();
    if (this.handleKeyboardHeightChange && wx.offKeyboardHeightChange) {
      wx.offKeyboardHeightChange(this.handleKeyboardHeightChange);
    }
  },

  setupKeyboardListener() {
    if (!wx.onKeyboardHeightChange) {
      return;
    }
    this.handleKeyboardHeightChange = (result) => {
      const height = result && result.height ? result.height : 0;
      this.setData({
        keyboardHeight: height > 0 ? height + 16 : 108,
        scrollExtraHeight: height > 0 ? height : 0,
      });
      this.scrollToBottom();
    };
    wx.onKeyboardHeightChange(this.handleKeyboardHeightChange);
  },

  handleInput(event) {
    this.setData({ draft: event.detail.value });
  },

  handleFocus() {
    this.scrollToBottom();
  },

  handleBlur() {
    this.setData({
      keyboardHeight: 108,
      scrollExtraHeight: 0,
    });
  },

  async initializeConversation() {
    this.setData({ bootstrapping: true });
    try {
      const [bootstrap, profile] = await Promise.all([
        fetchConversationBootstrap(),
        fetchAssistantProfile(),
      ]);
      this.setData({
        assistantStatus: profile.statusText || bootstrap.assistantStatus || '在线陪伴中',
        assistantIntro: profile.intro || '',
        conversationId: bootstrap.conversationId || '',
        messages: bootstrap.messages || [],
        hasMoreHistory: !!bootstrap.hasMoreHistory,
        nextMessageId: bootstrap.nextMessageId || this.getNextMessageId(bootstrap.messages || []),
      });
      this.scrollToBottom();
    } catch (error) {
      wx.showToast({
        title: '会话初始化失败',
        icon: 'none',
      });
    } finally {
      this.setData({ bootstrapping: false });
    }
  },

  showAssistantIntro() {
    wx.showModal({
      title: '关于小元',
      content:
        this.data.assistantIntro ||
        '小元提供情绪支持会话服务，支持会话初始化、消息发送、历史查询和个性化设置同步。',
      showCancel: false,
      confirmText: '知道了',
    });
  },

  async loadOlderMessages() {
    if (!this.data.hasMoreHistory) {
      wx.showToast({
        title: '没有更多历史了',
        icon: 'none',
      });
      return;
    }

    try {
      const response = await fetchConversationHistory({
        conversationId: this.data.conversationId,
        anchorMessageId: this.data.messages.length ? this.data.messages[0].id : '',
      });
      const mergedMessages = (response.messages || []).concat(this.data.messages);
      this.setData({
        messages: mergedMessages,
        hasMoreHistory: !!response.hasMoreHistory,
      });
    } catch (error) {
      wx.showToast({
        title: '历史消息加载失败',
        icon: 'none',
      });
    }
  },

  copyMessage(event) {
    const content = event.currentTarget.dataset.content;
    if (!content) {
      return;
    }
    wx.setClipboardData({
      data: content,
    });
  },

  async sendMessage() {
    const content = this.data.draft.trim();
    if (!content || this.data.sending) {
      return;
    }

    const baseId = this.data.nextMessageId;
    const userMessage = this.createMessage(baseId, 'user', content);
    const loadingMessage = this.createMessage(baseId + 1, 'assistant', '...', true);
    const nextMessages = this.data.messages.concat(userMessage, loadingMessage);

    this.setData({
      draft: '',
      sending: true,
      assistantStatus: '正在思考',
      messages: nextMessages,
      nextMessageId: baseId + 2,
    });

    this.scrollToBottom();

    try {
      const response = await sendConversationMessage({
        conversationId: this.data.conversationId,
        content,
        sceneCode: 'emotion_support_chat',
      });
      const replyMessage = response.replyMessage || this.createMessage(loadingMessage.id, 'assistant', '小元已收到你的消息。');
      const finalizedMessages = this.data.messages.map((item) => {
        if (item.id === loadingMessage.id) {
          return {
            id: replyMessage.id || item.id,
            role: 'assistant',
            content: replyMessage.content,
            time: replyMessage.time || this.getTimeLabel(),
            loading: false,
          };
        }
        return item;
      });

      this.setData({
        messages: finalizedMessages,
        sending: false,
        assistantStatus: response.assistantStatus || '在线陪伴中',
        conversationId: response.conversationId || this.data.conversationId,
      });

      this.scrollToBottom();
    } catch (error) {
      const finalizedMessages = this.data.messages.map((item) => {
        if (item.id === loadingMessage.id) {
          return {
            id: item.id,
            role: 'assistant',
            content: '消息发送失败，请稍后重试。',
            time: this.getTimeLabel(),
            loading: false,
          };
        }
        return item;
      });
      this.setData({
        messages: finalizedMessages,
        sending: false,
        assistantStatus: '在线陪伴中',
      });
    }
  },

  createMessage(id, role, content, loading = false) {
    return {
      id,
      role,
      content,
      time: this.getTimeLabel(),
      loading,
    };
  },

  getTimeLabel() {
    const date = new Date();
    const hours = `${date.getHours()}`.padStart(2, '0');
    const minutes = `${date.getMinutes()}`.padStart(2, '0');
    return `${hours}:${minutes}`;
  },

  scrollToBottom() {
    const messages = this.data.messages;
    if (!messages.length) {
      return;
    }
    const lastMessage = messages[messages.length - 1];
    setTimeout(() => {
      this.setData({
        scrollIntoView: `msg-${lastMessage.id}`,
      });
    }, 60);
  },

  getNextMessageId(messages) {
    if (!messages.length) {
      return 1;
    }
    return Math.max.apply(
      null,
      messages.map((item) => item.id)
    ) + 1;
  },

  clearReplyTimer() {
    if (this.replyTimer) {
      clearTimeout(this.replyTimer);
      this.replyTimer = null;
    }
  },
});
```

### pages/chat/index.json

```json
{
  "usingComponents": {
    "bottom-tabs": "/components/bottom-tabs/index"
  }
}
```

### pages/chat/index.wxml

```xml
<view class="page-shell chat-page">
  <view class="chat-header">
    <view class="brand-block">
      <view class="brand-avatar" bindtap="showAssistantIntro">
        <text class="brand-avatar__text">元</text>
      </view>
      <view class="brand-copy">
        <text class="brand-name">小元</text>
        <text class="brand-subtitle">癌症情绪支持陪伴助手</text>
      </view>
    </view>
    <view class="status-pill">
      <text>{{assistantStatus}}</text>
    </view>
  </view>

  <view class="chat-board card">
    <view class="board-toolbar">
      <view class="toolbar-copy">
        <text class="toolbar-title">安心会话</text>
        <text class="toolbar-subtitle">支持文本与表情，后续可扩展语音和图片</text>
      </view>
      <view class="toolbar-chip" bindtap="loadOlderMessages">加载历史</view>
    </view>

    <scroll-view
      class="message-scroll"
      style="height: calc(100vh - 454rpx - {{scrollExtraHeight}}px);"
      scroll-y
      enhanced
      show-scrollbar="{{false}}"
      scroll-into-view="{{scrollIntoView}}"
    >
      <view class="message-list">
        <view wx:if="{{hasMoreHistory}}" class="history-anchor" bindtap="loadOlderMessages">
          <text>查看更早消息</text>
        </view>

        <view
          wx:for="{{messages}}"
          wx:key="id"
          id="msg-{{item.id}}"
          class="message-row message-row--{{item.role}}"
        >
          <view
            class="bubble bubble--{{item.role}} {{item.loading ? 'bubble--loading' : ''}}"
            data-content="{{item.content}}"
            bindlongpress="copyMessage"
          >
            <view wx:if="{{item.loading}}" class="typing-dots">
              <text class="typing-dot"></text>
              <text class="typing-dot"></text>
              <text class="typing-dot"></text>
            </view>
            <text wx:else selectable="true" class="bubble-text">{{item.content}}</text>
          </view>
          <text class="message-time">{{item.time}}</text>
        </view>
      </view>
    </scroll-view>
  </view>

  <view class="composer-shell" style="bottom: calc({{keyboardHeight}}px + env(safe-area-inset-bottom));">
    <view class="composer card">
      <view class="input-wrap">
        <textarea
          class="composer-input"
          value="{{draft}}"
          maxlength="300"
          cursor-spacing="24"
          auto-height
          show-confirm-bar="{{false}}"
          placeholder="把现在的感受告诉小元..."
          placeholder-class="composer-placeholder"
          bindinput="handleInput"
          bindfocus="handleFocus"
          bindblur="handleBlur"
          bindconfirm="sendMessage"
        />
      </view>
      <view class="composer-actions">
        <view class="record-hint">长按可扩展录音</view>
        <button class="send-button" bindtap="sendMessage" disabled="{{sending}}">
          {{sending ? '发送中' : '发送'}}
        </button>
      </view>
    </view>
  </view>

  <bottom-tabs current="chat" />
</view>
```

### pages/chat/index.wxss

```css
.chat-page {
  padding-left: 24rpx;
  padding-right: 24rpx;
  padding-bottom: 300rpx;
  padding-top: 160rpx;
}

.chat-header {
  position: fixed;
  left: 24rpx;
  right: 24rpx;
  top: calc(env(safe-area-inset-top) + 24rpx);
  z-index: 20;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 22rpx 24rpx;
  border-radius: 30rpx;
  background: rgba(255, 250, 244, 0.86);
  border: 2rpx solid rgba(255, 138, 61, 0.14);
  backdrop-filter: blur(12rpx);
}

.brand-block {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.brand-avatar {
  width: 88rpx;
  height: 88rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: linear-gradient(135deg, #ffb55e 0%, #ff7c4d 50%, #5a9bff 100%);
  box-shadow: 0 12rpx 30rpx rgba(255, 138, 61, 0.22);
}

.brand-avatar__text {
  color: #ffffff;
  font-size: 34rpx;
  font-weight: 700;
}

.brand-copy {
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.brand-name {
  font-size: 34rpx;
  font-weight: 700;
}

.brand-subtitle {
  font-size: 22rpx;
  color: var(--text-secondary);
}

.status-pill {
  padding: 12rpx 18rpx;
  border-radius: 999rpx;
  background: rgba(90, 155, 255, 0.12);
  color: var(--brand-blue);
  font-size: 22rpx;
  font-weight: 600;
}

.chat-board {
  overflow: hidden;
}

.board-toolbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 20rpx;
  padding: 28rpx 28rpx 0;
}

.toolbar-copy {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.toolbar-title {
  font-size: 30rpx;
  font-weight: 700;
}

.toolbar-subtitle {
  font-size: 22rpx;
  color: var(--text-secondary);
}

.toolbar-chip {
  flex-shrink: 0;
  padding: 14rpx 20rpx;
  border-radius: 999rpx;
  background: var(--bg-soft-orange);
  color: var(--brand-orange-deep);
  font-size: 22rpx;
}

.message-scroll {
  height: calc(100vh - 454rpx);
  min-height: 720rpx;
}

.message-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  padding: 24rpx 28rpx 200rpx;
}

.history-anchor {
  align-self: center;
  padding: 12rpx 22rpx;
  border-radius: 999rpx;
  background: rgba(242, 244, 247, 0.9);
  color: var(--text-secondary);
  font-size: 22rpx;
}

.message-row {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}

.message-row--user {
  align-items: flex-start;
}

.message-row--assistant {
  align-items: flex-end;
}

.bubble {
  max-width: 80%;
  padding: 20rpx 22rpx;
  border-radius: 28rpx;
  word-break: break-word;
  white-space: pre-wrap;
  font-size: 30rpx;
  line-height: 1.5;
}

.bubble--user {
  background: var(--bg-soft-gray);
  color: var(--text-primary);
  border-top-left-radius: 10rpx;
}

.bubble--assistant {
  background: var(--bg-soft-blue);
  color: #214468;
  border-top-right-radius: 10rpx;
  box-shadow: var(--shadow-bubble);
}

.bubble--loading {
  min-width: 110rpx;
}

.bubble-text {
  display: block;
}

.message-time {
  font-size: 22rpx;
  color: var(--text-light);
}

.typing-dots {
  display: flex;
  align-items: center;
  gap: 10rpx;
  padding: 8rpx 4rpx;
}

.typing-dot {
  width: 14rpx;
  height: 14rpx;
  border-radius: 50%;
  background: rgba(90, 155, 255, 0.7);
  animation: bounce 1.1s infinite ease-in-out;
}

.typing-dot:nth-child(2) {
  animation-delay: 0.12s;
}

.typing-dot:nth-child(3) {
  animation-delay: 0.24s;
}

@keyframes bounce {
  0%,
  80%,
  100% {
    opacity: 0.25;
    transform: translateY(0);
  }

  40% {
    opacity: 1;
    transform: translateY(-8rpx);
  }
}

.composer-shell {
  position: fixed;
  left: 24rpx;
  right: 24rpx;
  z-index: 25;
}

.composer {
  padding: 18rpx;
}

.input-wrap {
  padding: 6rpx 8rpx 2rpx;
  border-radius: 24rpx;
  background: #ffffff;
}

.composer-input {
  width: 100%;
  min-height: 88rpx;
  max-height: 220rpx;
  padding: 20rpx;
  font-size: 30rpx;
  line-height: 1.5;
}

.composer-placeholder {
  color: var(--text-light);
}

.composer-actions {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 16rpx;
  margin-top: 16rpx;
}

.record-hint {
  font-size: 22rpx;
  color: var(--text-secondary);
}

.send-button {
  min-width: 152rpx;
  height: 74rpx;
  margin: 0;
  padding: 0 26rpx;
  border: 0;
  border-radius: 999rpx;
  background: linear-gradient(135deg, #ff8a3d 0%, #5a9bff 100%);
  color: #ffffff;
  font-size: 28rpx;
  font-weight: 600;
  line-height: 74rpx;
}

.send-button::after {
  border: none;
}
```

### pages/history/index.js

```javascript
const { fetchSessionList } = require('../../services/history');

Page({
  data: {
    sessions: [],
  },

  onLoad() {
    this.loadSessions();
  },

  async loadSessions() {
    try {
      const response = await fetchSessionList();
      this.setData({
        sessions: response.sessions || [],
      });
    } catch (error) {
      wx.showToast({
        title: '历史会话加载失败',
        icon: 'none',
      });
    }
  },
});
```

### pages/history/index.json

```json
{
  "usingComponents": {
    "bottom-tabs": "/components/bottom-tabs/index"
  }
}
```

### pages/history/index.wxml

```xml
<view class="page-shell history-page">
  <view class="hero card">
    <text class="hero-title">历史会话</text>
    <text class="hero-subtitle">先用 mock 列表模拟最近会话，后续可接分页和搜索。</text>
  </view>

  <view class="session-list">
    <view wx:for="{{sessions}}" wx:key="id" class="session-card card">
      <view class="session-main">
        <text class="session-title">{{item.title}}</text>
        <text class="session-desc">{{item.preview}}</text>
      </view>
      <view class="session-meta">
        <text class="session-time">{{item.time}}</text>
        <view class="session-tag {{item.moodClass}}">{{item.mood}}</view>
      </view>
    </view>
  </view>

  <bottom-tabs current="history" />
</view>
```

### pages/history/index.wxss

```css
.history-page {
  padding: 24rpx 24rpx 180rpx;
}

.hero {
  padding: 28rpx;
  background: linear-gradient(135deg, rgba(255, 239, 220, 0.98) 0%, rgba(232, 243, 255, 0.98) 100%);
}

.hero-title {
  display: block;
  font-size: 34rpx;
  font-weight: 700;
}

.hero-subtitle {
  display: block;
  margin-top: 10rpx;
  color: var(--text-secondary);
  font-size: 24rpx;
}

.session-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  margin-top: 24rpx;
}

.session-card {
  display: flex;
  justify-content: space-between;
  gap: 20rpx;
  padding: 28rpx;
}

.session-main {
  flex: 1;
}

.session-title {
  display: block;
  font-size: 30rpx;
  font-weight: 700;
}

.session-desc {
  display: block;
  margin-top: 10rpx;
  color: var(--text-secondary);
  font-size: 24rpx;
  line-height: 1.5;
}

.session-meta {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 14rpx;
}

.session-time {
  color: var(--text-light);
  font-size: 22rpx;
}

.session-tag {
  padding: 10rpx 18rpx;
  border-radius: 999rpx;
  font-size: 22rpx;
}

.session-tag--warm {
  background: var(--bg-soft-orange);
  color: var(--brand-orange-deep);
}

.session-tag--calm {
  background: var(--bg-soft-blue);
  color: var(--brand-blue);
}
```

### pages/profile/index.js

```javascript
const { fetchProfileCards } = require('../../services/profile');

Page({
  data: {
    items: [],
  },

  onLoad() {
    this.loadProfileCards();
  },

  async loadProfileCards() {
    try {
      const response = await fetchProfileCards();
      this.setData({
        items: response.items || [],
      });
    } catch (error) {
      wx.showToast({
        title: '个人资料加载失败',
        icon: 'none',
      });
    }
  },
});
```

### pages/profile/index.json

```json
{
  "usingComponents": {
    "bottom-tabs": "/components/bottom-tabs/index"
  }
}
```

### pages/profile/index.wxml

```xml
<view class="page-shell profile-page">
  <view class="profile-hero card">
    <view class="profile-avatar">元</view>
    <view class="profile-copy">
      <text class="profile-name">我的</text>
      <text class="profile-subtitle">管理偏好、情绪陪伴方式与会话设置</text>
    </view>
  </view>

  <view class="profile-section">
    <view wx:for="{{items}}" wx:key="title" class="profile-item card">
      <view>
        <text class="profile-item__title">{{item.title}}</text>
        <text class="profile-item__desc">{{item.desc}}</text>
      </view>
      <text class="profile-item__action">{{item.action}}</text>
    </view>
  </view>

  <bottom-tabs current="profile" />
</view>
```

### pages/profile/index.wxss

```css
.profile-page {
  padding: 24rpx 24rpx 180rpx;
}

.profile-hero {
  display: flex;
  align-items: center;
  gap: 20rpx;
  padding: 28rpx;
  background: linear-gradient(135deg, rgba(255, 246, 235, 0.98) 0%, rgba(237, 245, 255, 0.98) 100%);
}

.profile-avatar {
  width: 96rpx;
  height: 96rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: linear-gradient(135deg, #ff9936 0%, #5a9bff 100%);
  color: #ffffff;
  font-size: 36rpx;
  font-weight: 700;
}

.profile-copy {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.profile-name {
  font-size: 34rpx;
  font-weight: 700;
}

.profile-subtitle {
  color: var(--text-secondary);
  font-size: 24rpx;
}

.profile-section {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  margin-top: 24rpx;
}

.profile-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 16rpx;
  padding: 26rpx 28rpx;
}

.profile-item__title {
  display: block;
  font-size: 30rpx;
  font-weight: 700;
}

.profile-item__desc {
  display: block;
  margin-top: 10rpx;
  color: var(--text-secondary);
  font-size: 24rpx;
}

.profile-item__action {
  color: var(--brand-orange-deep);
  font-size: 24rpx;
  font-weight: 600;
}
```

### project.config.json

```json
{
  "appid": "wx4e5c1f13b4d7b916",
  "projectname": "XiaoyuanAI-wx",
  "compileType": "miniprogram",
  "miniprogramRoot": "./",
  "srcMiniprogramRoot": "./",
  "condition": {},
  "setting": {
    "es6": true,
    "enhance": true,
    "postcss": true,
    "minified": true,
    "urlCheck": false,
    "coverView": true,
    "compileHotReLoad": true,
    "ignoreUploadUnusedFiles": true,
    "compileWorklet": false,
    "uglifyFileName": false,
    "uploadWithSourceMap": true,
    "packNpmManually": false,
    "packNpmRelationList": [],
    "minifyWXSS": true,
    "minifyWXML": true,
    "localPlugins": false,
    "disableUseStrict": false,
    "useCompilerPlugins": false,
    "condition": false,
    "swc": false,
    "disableSWC": true,
    "babelSetting": {
      "ignore": [],
      "disablePlugins": [],
      "outputPath": ""
    }
  },
  "simulatorPluginLibVersion": {},
  "packOptions": {
    "ignore": [],
    "include": []
  },
  "editorSetting": {}
}
```

### sitemap.json

```json
{
  "desc": "mock sitemap",
  "rules": []
}
```

